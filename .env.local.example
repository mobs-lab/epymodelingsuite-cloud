# Local configuration for development
# Copy this to .env.local and fill in your values
# NEVER commit .env.local to version control!
# Source with: source .env.local

# GitHub Personal Access Token for local Docker builds
# This is only needed for local builds that include the modeling suite
# Get your PAT from: https://github.com/settings/tokens
export GITHUB_PAT=your_github_pat_here

# Local execution configuration
export SIM_ID=test-sim
export RUN_ID=run-20241015

# DIR_PREFIX is defined in .env (e.g., "pipeline/flu/")
# Storage paths are automatically constructed as:
#   Local mode: /data/bucket/{DIR_PREFIX}/{SIM_ID}/{RUN_ID}/{inputs|results}/
#   Cloud mode: gs://{GCS_BUCKET}/{DIR_PREFIX}/{SIM_ID}/{RUN_ID}/{inputs|results}/
