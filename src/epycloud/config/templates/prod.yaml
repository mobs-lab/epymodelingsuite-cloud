# Production environment overrides

google_cloud:
  # Override project for prod environment
  # project_id: modeling-prod

  batch:
    max_parallelism: 100  # Full parallelism

    stage_b:
      cpu_milli: 4000  # Larger instances
      memory_mib: 16384
      # machine_type: "c4d-standard-4"  # Optional: compute-optimized

logging:
  level: INFO
  storage_verbose: false  # Less verbose in prod
